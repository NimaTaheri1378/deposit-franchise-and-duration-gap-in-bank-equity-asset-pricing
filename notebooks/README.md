# Notebooks

Exploratory notebooks are intentionally kept out of the core pipeline. Any
notebook added here should be a thin demo or audit layer that reads generated
artifacts and does not contain credentials, raw WRDS data, or one-off modelling
state.
