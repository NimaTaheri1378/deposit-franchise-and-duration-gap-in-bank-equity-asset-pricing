# Paper Scaffold

The project includes a `paper/paper.qmd` scaffold because the proposal requires a
paper-style report path. Full manuscript writing is intentionally deferred. Use
`make paper` to render a public-safe HTML report from manifests and summary
tables after a completed pipeline run.
